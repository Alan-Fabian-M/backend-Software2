fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
opencv-python-headless>=4.10.0
numpy>=1.26.0
pillow>=10.0.0
pytest>=8.0.0
httpx>=0.27.0

# Scene Compiler (Fase 2: furniture detection, OCR scale reading)
ultralytics>=8.3.0
pytesseract>=0.3.10
pydantic>=2.0.0

# pytesseract needs the Tesseract OCR *binary* installed separately at the OS level:
#   Fedora/RHEL: sudo dnf install tesseract
#   Debian/Ubuntu: sudo apt install tesseract-ocr
#   macOS: brew install tesseract
